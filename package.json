{
  "name": "facturaexpress",
  "version": "1.0.0",
  "private": true,
  "description": "FacturaExpress - Monorepositorio (API REST + Frontend Angular)",
  "scripts": {
    "setup": "npm install && npm run install:all && npm run db:setup",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:api": "npm run dev --prefix backend",
    "dev:web": "npm start --prefix frontend",
    "start": "npm run dev",
    "db:setup": "npm run db:setup --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:seed": "npm run db:seed --prefix backend",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
